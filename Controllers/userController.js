export const registerUser = async (req, res) => {
  res.status(200).json({ status: 'success', message: 'Registration controller placeholder' });
};

export const loginUser = async (req, res) => {
  res.status(200).json({ status: 'success', message: 'Login controller placeholder' });
};
